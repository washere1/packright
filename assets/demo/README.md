# Generated demo assets

Everything in this directory is synthetic test/demo data, not a real scan. Run
`python scripts/generate_fixtures.py` to reproduce the GLB files and malformed
inputs. Source GLB coordinates are meters; the application contract converts to
millimeters exactly once.

