export function PackingStepper({ step, total, xray, meshes, onStep, onXray, onMeshes }: { step: number; total: number; xray: boolean; meshes: boolean; onStep: (step: number) => void; onXray: () => void; onMeshes: () => void }) {
  return <div className="viewer-controls"><button className="secondary" onClick={() => onStep(Math.max(0, step - 1))} disabled={!step}>Back</button><button className="secondary" onClick={() => onStep(Math.min(total, step + 1))} disabled={step >= total}>Next item</button><button className="secondary" onClick={() => onStep(total)}>Show all</button><button className="secondary" onClick={onXray}>{xray ? "Solid view" : "X-ray view"}</button><button className="secondary" onClick={onMeshes}>{meshes ? "Cuboids (fast)" : "Model previews"}</button></div>;
}
